# Explain It — streams1

Explain to a teammate what stream entry IDs represent and why they're useful.

Write your explanation in `feynman/explanations/streams1.md`.

Cover:
- The format of a stream ID (what are the two parts?)
- What happens if two entries are added in the same millisecond?
- Why "0" means "beginning" and "$" means "end/now"
- A practical use case for reading from a specific timestamp ID

QUALITY CHECK: Include an example of what a real stream ID looks like (e.g., "1711234567890-0").
